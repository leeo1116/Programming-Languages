from functools import cmp_to_key

class Solution(object):
    def largest_number(self, num):
        num = list(map(str, num))
        sorted(num, key=)
        return "".join(num)

s = Solution()
print(s.largest_number([3, 1, 4, 5, 23]))